const Error = ({children}) => {

    return (
        
        <div className="text-center bg-red-500 uppercase text-white font-bold p-2 rounded-md mb-4">
            {children}
        </div>
    )
}

export default Error
